<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fepeo.boot.festival.model.mapper.FestivalMapper">
	
	<insert id="insertFestival" parameterType="Festival">
        INSERT INTO FESTIVAL 
        VALUES (
            SEQ_FESTIVAL_NO.NEXTVAL,
            #{festivalName},
            #{festivalStartDate},
            #{festivalEndDate},
            #{festivalFileName},
            #{festivalFileRename},
            #{festivalFilePath},
            #{festivalPhone},
            #{festivalAddress},
            #{festivalDetailAddress},
            #{mapVCode},
            #{mapHcode}
        )
    </insert>

    <select id="selectFestivalList" resultType="com.fepeo.boot.festival.model.vo.Festival">
	    SELECT
	        FESTIVAL_NO,FESTIVAL_NAME,FESTIVAL_START_DATE,FESTIVAL_END_DATE,FESTIVAL_FILE_NAME,
	        FESTIVAL_FILE_RENAME, FESTIVAL_FILE_PATH,FESTIVAL_PHONE,FESTIVAL_ADDRESS,FESTIVAL_DETAIL_ADDRESS,
	        MAP_V_CODE, MAP_H_CODE  
	    FROM FESTIVAL
	        ORDER BY FESTIVAL_START_DATE DESC
	</select>
	<select id="selectFestivalByNo" resultType="com.fepeo.boot.festival.model.vo.Festival">
		SELECT * FROM festival WHERE FESTIVAL_NO = #{festivalNo}
	</select>
	
</mapper>