<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fepeo.boot.member.model.mapper.MemberMapper">

	<insert id="insertMember">
		INSERT INTO MEMBER VALUES(SEQ_MEMBER_NO.NEXTVAL
		,#{memberId}, #{memberPw}, NULL, #{memberName}
		,#{nickname}, #{email}, #{address}, #{gender}
		,NULL, NULL, NULL, NULL, DEFAULT, DEFAULT, DEFAULT
		,DEFAULT)
	</insert>
	<select id="memberLogin" resultType="Member">
		SELECT MEMBER_ID, MEMBER_NAME, NICKNAME, EMAIL, ADDRESS, GENDER FROM MEMBER 
		WHERE MEMBER_ID = #{memberId} AND MEMBER_PW = #{memberPw}
	</select>

</mapper>